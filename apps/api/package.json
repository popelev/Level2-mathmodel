{
  "name": "level2-mathmodel-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "TypeScript BFF mock for mathmodel OpenAPI (Wave 1)",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/server.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
