"""API app stub — Wave 0 only; real routes in later waves."""

from __future__ import annotations


def create_app():
    """Return a placeholder app object until ASGI framework is wired."""
    return {"service": "level2-mathmodel", "mode": "scaffold"}
