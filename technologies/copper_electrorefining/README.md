# Copper electrorefining (placeholder)

Technology pack for copper electrorefining monitoring + planning.

**Wave 0:** directory reserved only — no models, mappings, or UI yet.

Later waves will add:

- Tag mapping from Level2 read-only inputs
- Local planning variables (engine-owned; never PLC write)
- Domain plugins under `services/engine/.../plugins`
